# --- PeerJS / WebRTC configuration (Vite env vars) ---
# Copy this file to `.env.local` (recommended) or `.env`.
#
# English:
#   - VITE_* variables are exposed to the client bundle (do NOT put secrets here).
#   - TURN credentials SHOULD NOT be committed. Use `.env.local` and keep it out of git.
#
# 日本語:
#   - VITE_* はクライアントに埋め込まれます（秘密情報を入れないでください）。
#   - TURN のユーザー名/パスワードはコミットしないでください。.env.local を使うのがおすすめです。

# PeerServer override (signaling). Leave empty to use PeerJS Cloud default.
VITE_PEERJS_HOST=
VITE_PEERJS_PORT=
VITE_PEERJS_PATH=
VITE_PEERJS_SECURE=

# WebRTC ICE servers (JSON). When empty, the browser/PeerJS defaults are used.
# Example (TURN over TLS on 443 is the usual "works on school Wi-Fi" option):
# VITE_WEBRTC_ICE_SERVERS=[
#   {"urls":["stun:stun.l.google.com:19302"]},
#   {"urls":["turns:turn.example.com:443?transport=tcp"],"username":"user","credential":"pass"}
# ]
VITE_WEBRTC_ICE_SERVERS=

# Optional: "relay" forces TURN-only (useful on strict networks).
# VITE_WEBRTC_ICE_TRANSPORT_POLICY=relay
VITE_WEBRTC_ICE_TRANSPORT_POLICY=

# Optional PeerJS debug level (0-3). Try 3 when diagnosing.
# VITE_PEERJS_DEBUG=3
VITE_PEERJS_DEBUG=

# Network transport:
# - peerjs : WebRTC/PeerJS only
# - wsrelay: force WebSocket relay
# - auto   : keep PeerJS as primary and auto-fallback to wsrelay on signaling error
VITE_NETWORK_TRANSPORT=auto

# Optional WebSocket relay URL (for restrictive networks).
# Example:
# VITE_WS_RELAY_URL=ws://localhost:8787
# VITE_WS_RELAY_URL=wss://relay.example.com
VITE_WS_RELAY_URL=
